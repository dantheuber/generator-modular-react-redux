import baseSelect from '../util/base-select';

const select = baseSelect('router');

export const result = select('result');
export const pathname = select('pathname');
export const key = select('key');
export const params = select('params');
